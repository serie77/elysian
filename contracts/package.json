{
  "name": "contracts",
  "private": true,
  "version": "0.1.0",
  "description": "Elysian shielded pool, sealed batch swap adapter and verifiers for Robinhood Chain.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network robinhoodTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network robinhood",
    "node": "hardhat node",
    "test:fork": "hardhat test test-fork/fork.test.ts"
  },
  "devDependencies": {
    "@elysian/core": "*",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.6.1",
    "circomlibjs": "^0.1.7",
    "dotenv": "^17.2.0",
    "hardhat": "^2.29.1",
    "snarkjs": "^0.7.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.0"
  }
}
