/**
 * Contract address of the Elysian token. Empty until launch: paste it here, or set
 * NEXT_PUBLIC_TOKEN_CA on the host and redeploy, and the box on the landing page fills in.
 */
export const TOKEN_CA: string = process.env.NEXT_PUBLIC_TOKEN_CA ?? '';
